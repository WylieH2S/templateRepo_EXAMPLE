#AI:1
TYPE="ai_rules"
SCOPE="project-wide"
BUDGET="≤100 lines — keep this file short"

PURPOSE:
Project-wide hard constraints. Loaded every session as Tier 1.
Do not add project narrative here — that lives in ai_context/START_HERE.md and readme_AI.ai.

---

SESSION_START_CHECKLIST:
Every session, before any work:
1. Read ai_context/START_HERE.md -- follow the boot sequence
2. Read ai_context/CURRENT_MISSION.md -- restate active scope before choosing work
3. Read WORKSHEET.human -- check for test reports, answered questions, or notes
4. Skim recent entries in AI_HANDOFF.ai -- recover latest session context
5. Act on any new input; move handled worksheet items to HISTORY
6. Update the CURRENT VERSION line in WORKSHEET.human if stale

---

CODE_CHANGE_GATE:
Before any code change, fill this contract in your response or working notes:

  Task:
  Blocker:
  Design intent served:
  Subsystem:
  Files likely touched:
  Explicitly not touching:
  Expected result:
  Live test:
  Revert path:

If the contract is fuzzy, stop instead of coding.

---

WRITEBACK_RULES:
When a decision, experiment result, or clarification affects future correctness:
- Append a session entry to AI_HANDOFF.ai
- Update ai_context/current_state.md (version, status, open items)
- Log decisions in ai_context/decision_log.md if they remain in effect
- Update WORKSHEET.human CURRENT VERSION line if version changed
- Append only; do not rewrite history

---

EXPERIMENT_LOG_FORMAT:
Failed experiments must be logged with:
  change:    what was done
  expect:    what should have happened
  observe:   what actually happened
  repro:     how to reproduce
  revert:    how to undo
  location:  file or function
  status:    worked | failed | partial | unknown

---

PROJECT_SPECIFIC_RULES:
<!-- Replace with project-specific constraints:
     language version pins, forbidden patterns, required conventions. -->
- (none yet)
