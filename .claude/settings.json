{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Canonical SessionStart gate pair (HI Mode). Copied verbatim into each repo by the fleet rollout — see repoManager/.repo-manager/playbooks/sessionstart-gates-fleet-rollout.md. handoff-gate.sh = model/effort vs the latest HANDOFF recommendation; wrap-gate.sh = prior-session unwrapped/unpushed/uncommitted. Both are advisory (always exit 0). If a repo already has a settings.json, MERGE these hooks in rather than overwrite.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          { "type": "command", "command": "bash .claude/hooks/handoff-gate.sh" },
          { "type": "command", "command": "bash .claude/hooks/wrap-gate.sh" }
        ]
      },
      {
        "matcher": "resume",
        "hooks": [
          { "type": "command", "command": "bash .claude/hooks/handoff-gate.sh" },
          { "type": "command", "command": "bash .claude/hooks/wrap-gate.sh" }
        ]
      },
      {
        "matcher": "clear",
        "hooks": [
          { "type": "command", "command": "bash .claude/hooks/handoff-gate.sh" },
          { "type": "command", "command": "bash .claude/hooks/wrap-gate.sh" }
        ]
      },
      {
        "matcher": "compact",
        "hooks": [
          { "type": "command", "command": "bash .claude/hooks/handoff-gate.sh" },
          { "type": "command", "command": "bash .claude/hooks/wrap-gate.sh" }
        ]
      }
    ]
  }
}
